<%= form_for(@menu_dish) do |f| %>
  <% if @menu_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_dish.errors.count, "error") %> prohibited this menu_dish from being saved:</h2>

      <ul>
      <% @menu_dish.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
